<?php
include './utils/stack.php';
function simulate_people($peopleStack)
{
    $modified = new Stack();
    // $modified becomes the active stack
    if (rand(0, 1) % 2 == 0) {
        // push someone
    } else {
        // pop someone
    }
    return $modified;
}
